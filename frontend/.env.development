VITE_APP_TITLE=HRV Analysis App (Development)

# --- Appwrite Configuration ---
# Point to the local Appwrite instance, which is on port 8080
VITE_APPWRITE_ENDPOINT=http://localhost:8080/v1
VITE_APPWRITE_PROJECT_ID=hrv-app 